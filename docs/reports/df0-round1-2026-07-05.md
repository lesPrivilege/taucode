# DF0 Round 1 — Handoff to Cowork (2026-07-05)

Ended early by operator judgment once the first-day observation crystallized:
**context-window savings are large, but the agent-capability cost (re-reads) is also
large — "可能总会反复读."** Verification done; `pi/` zero-diff restored. This is the
retrospective input.

## Headline

Compaction trades context savings for agent capability, via re-reads. The operator's
call to end the round rests on that behavior, not on finishing the dashboard.

## Data — anecdotal AND contaminated (read with the caveat)

The DF0 loops ran at **`compact-after=500`, not the charter's 32k** — leftover from
the R5 F-A-ack check (`/compaction set compact-after=500`), never reset because we
didn't restart between launcher-testing and DF0 (my setup miss). So this is the
500-regime, not natural crossing:

- projected **110 / 116 turns** (~95%) — compaction on almost every turn;
- 20 paths compacted; compacted-path re-reads **56, rate ~0.64**;
- cache held: **CH 97.8%, 2.33M cache-read tokens.**

Two reframes (raw magnitudes, not conclusions — exact `reread_rate` semantics
unconfirmed):
1. **Re-reads may be the bigger net-save cost, not cache-break** — aggressive
   compaction saves tokens then pays them back as re-fetches.
2. **Deterministic compaction may preserve cache better than feared** — a stable
   compacted prefix re-warms, so the cache-break term may be small.

**Correction (found during handoff cleanup — the observation surface earning its
keep):** a later session `019f3204` ran at the clean default **32k** (no tuning
event) and crossed **naturally** (projected 91/145 turns, 17 paths), with
compacted-path re-read rate **~0.62** — essentially the 500-regime's 0.64. So the
high re-read behavior is **not a 500 artifact**; it holds at the charter baseline.
Its cache also held **CH 95.2%** through the natural crossing — corroborating the
cache-preserved reframe. Caveats: one session, it is the *failed*-L3 build (the
model may have been flailing), and it is a raw rate, not ≥5 categorized triage
entries. So the seed's ≥5-triage trigger is still unmet, but **both reframes now
have a clean-32k corroboration** — stronger than "no usable numbers."

Isolation: `019f31dc` (500-regime) moved to `ambient/contaminated/` with a
`regime500-contaminated` tag; `019f3204` (clean 32k) retained.

## L3 incident — hard-zone violation (dogfooding signal + my prompt gap)

Building L3, deepseek edited **pi core**: modified `pi/…/interactive-mode.ts` (+32 —
imported a `CompactGateWidget`, added holder/widget properties, wired
`extensionWidgetsAbove["__compaction_gate"]`) and created
`pi/…/components/compact-gate-widget.ts`. It abandoned L2's correct extension-side
`ctx.ui.setWidget` for a pi-core hack. **Reverted; `pi/` is zero-diff again.**

Two lessons:
- **Weak-model boundary adherence (wall 2):** pointed at pi source merely to check a
  render contract, the model drifted into implementing there. F-E (relative paths
  resolve under the pi package) makes that drift easy.
- **My prompt gap:** I sent it to read `interactive-mode.ts` with no explicit "edit
  ONLY the extension, never `pi/`" fence. Orchestrating a weak model requires fencing
  the hard zone in every prompt — it will not infer it.

This is the same capability wall the operator flagged, surfacing inside the build.

## Connection to `note-view-based-context.md`

The re-read finding feeds that seed directly: re-read reframed from pathology to
"fetching a view" (category-(a) directed re-read = normal operation). Our 64% is a
triage data point — but 500-regime and undirected, i.e. exactly the "病根是无定向
全文重读" the note hypothesizes as the negative-case root. The seed stays a seed
(needs ≥5 clean triage entries); this round didn't produce them.

## Dashboard build status

- **L1** (pi UI API scan): done — feasible, no `pi/` changes; surfaces =
  `setWidget` / `setStatus` / `custom()`.
- **L2** (extension gate strip via `ctx.ui.setWidget`): built, 46 tests pass,
  `string[]` render confirmed correct by L3's investigation. **Render not visually
  verified; preserved UNCOMMITTED** in `extensions/deterministic-compaction/src/
  extension.ts`. Minor: possible unused `Box`/`Text` import.
- **L3** (enrich): FAILED — hard-zone violation, reverted. Build **suspended.**

## State at handoff

- `pi/`: zero-diff restored (verified clean).
- `extensions/…/extension.ts`: uncommitted L2 (correct architecture, unverified render).
- Untracked (Cowork's, uncommitted): `docs/df0-charter.md`, `docs/note-view-based-context.md`.
- Committed this round: `df0-compaction-dashboard-design.md` (`2c67142`).

## For Cowork — decisions

1. **Rule on view-based-context vs the ledger-dashboard plan.** The re-read finding
   may redirect the whole DF0/dashboard purpose (net-flow rewrite: hold-full-text ×N
   vs one-time directed re-read).
2. **L2's fate:** commit (correct architecture, useful standalone strip) / verify / discard.
3. **If DF0 resumes:** must start fresh (restart → 32k) and fence the hard zone
   explicitly in every weak-model prompt.
4. Commit the two untracked Cowork docs.
