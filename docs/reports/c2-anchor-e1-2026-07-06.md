# C2-ANCHOR-E1 Execution Log — 2026-07-06

Scope: E1 x C'' (`TAUCODE_SEMANTIC_ANCHOR=1`), n=3, real DeepSeek, same packet and
R2-style audit surface. Scenario: `G2-E1`; arm: physical `C`; compact-after:
32000; keep-recent: 3; workspace: empty.

## Harness Fix Before Run

Pre-run audit found that `experiments/run.ts` used `TAUCODE_SEMANTIC_ANCHOR=1` to
fill `TAUCODE_ANCHOR_ACCEPTANCE`, but did not pass the env flag into
`installDeterministicCompaction`. That would have produced a false C'' run. The
runner now ORs env flags with addon-arm flags and records the effective flags in
JSONL meta.

Verification before real runs:

- `cd experiments && npm test && npm run typecheck` passed.
- `cd extensions/deterministic-compaction && npm test && npm run typecheck`
  passed after the placebo-tail preflight addition.

## Runs

Output directory: `experiments/results/c2-anchor-e1/`.

| Repeat | JSONL | Turns | Projected | Static | Re-reads | Comp-path re-reads | Total tokens | Cache read |
| ---: | --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 1 | `01-r1-G2-E1-Cpp.jsonl` | 24 | 11 | 4/5 | 5 | 4 | 739,901 | 649,216 |
| 2 | `02-r2-G2-E1-Cpp.jsonl` | 17 | 7 | 5/5 | 2 | 0 | 437,460 | 327,808 |
| 3 | `03-r3-G2-E1-Cpp.jsonl` | 30 | 15 | 0/5 | 5 | 2 | 926,141 | 803,328 |

Meta scan:

- provider: `deepseek` for all 3.
- data_kind: `g2-packet-run` for all 3.
- `mechanism.extension_flags.semantic_anchor`: `true` for all 3.
- `anchor_acceptance_targets`: `SUBSYSTEM-MAP.md` for all 3.
- artifact row present for all 3.

Acceptance detail:

- r1 wrote `SUBSYSTEM-MAP.md` but missed the `extensions/loader.ts:\d+` regex
  (it used `loader.ts:...` without the path segment).
- r2 passed 5/5.
- r3 did not produce `SUBSYSTEM-MAP.md` at all.

## Comparison Frame

R2 E1 references:

| Arm | Static outcomes | Median total tokens |
| --- | --- | ---: |
| A | 0/5, 5/5, 5/5 | 1,398,713 |
| B' | 4/5, 5/5, 5/5 | 1,179,961 |
| C | 4/5, 0/5, 4/5 | 1,484,603 |
| D | 5/5, 5/5, 5/5 | 2,245,171 |
| C'' | 4/5, 5/5, 0/5 | 739,901 |

Compare output:

- `experiments/results/c2-anchor-e1/G2-E1-c2-vs-r2-compare.md`

## Gate Result

C'' is cheaper than the old E1 arms on median total tokens, but it did not
achieve D-grade completion (`5/5 x3`). The gate therefore does **not** authorize
README narrative claims that C'' fixes exploration completion.

Branch implication: C'' is insufficient under the stated gate. Any Branch B
policy-arm run must still be treated as experimental and must include the C+PL
placebo control.
